# Load all branches because two remotes are a nightmare
git checkout -t "origin/java8"
git checkout -t "origin/java8-multiarch"
git checkout -t "origin/java16"
git checkout -t "origin/multiarch-latest"
git checkout master
